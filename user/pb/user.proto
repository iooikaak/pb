syntax = "proto3"; // 指定proto版本
package user;

// Gender 用户性别
enum Gender {
  // Unknow 未知
  Unknow = 0;
  // Male 男
  Male = 1;
  // 女
  Female = 2;
}

// stream 作为请求参数：OPEN API 无需认证
// stream 作为响应：内网API
// 无stream：APP
// 同时出现在请求和响应：只能服务间RPC访问
service User{

}